pipeline {
    agent any
stages {
        stage('Build') {
            withAWS(credentials: 'aws-keys', region: 'us-east-1') {
                sh'terraform init'
        }
        }
        
        stage('Test') {
            steps {
                echo 'Testing...'
                // Your test steps here
            }
        }
        
        stage('Deploy') {
            steps {
                echo 'Deploying...'
                // Your deployment steps here
            }
        }
    }
}